# default config
run_config:
  device: cuda
  dataset: compas
  metric: ACC
  max_epoch: 500
  learning_rate: 0.001
  batch_size: 512
  num_learner: 8
  train_ratio: 0.7
  validation_ratio: 0.1
  K_ratio: 0.3
  hidden_dim: 256
  random_state: 42
  early_stop_patience: 200
  num_layers: 5
wandb_config:
  use_wandb: True    
  entity: a220284268
  project: TabularGNN_V5_layer_test
  # project: TabularGNN ablation without GNN
  name : 3_fold with early stop
  # group: K_graph_Multi_noGraph 
  # group: KG_Multi normal
  group: KGML L=5 
  # group: MLP baseline
  notes: V5 small loss 9 7 5 3 1 res cat predict:MLP dropout b512 BN selfloop no EGW

# available datasets:
# adult
# compas
# compass_old
# covertype_num
# credit
# electricity_cat